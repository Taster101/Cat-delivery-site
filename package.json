{
  "name": "cat-delivery-site",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",

  "scripts": {
    "install": "cd server && npm i && cd ../client && npm i",
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "concurrently \"cd server && nodemon server.js\" \" cd client && npm start\"",
    "build": "cd client && npm run build",
    "server": "cd server && nodemon server.js",
    "client": "cd client && npm start",
    "start": "npm run build && cd server && node server.js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Taster101/Cat-delivery-site.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/Taster101/Cat-delivery-site/issues"
  },
  "homepage": "https://github.com/Taster101/Cat-delivery-site#readme",
  "devDependencies": {
    "concurrently": "^8.2.0",
    "nodemon": "^3.0.1"
  }
}
